# Extraction Status

The tar file extraction has been attempted. Please verify the following:

1. Check if `refactor/main` branch exists: `git branch -a | grep refactor`
2. Check if files were extracted: `git status`
3. If extraction didn't complete, run: `python3 _extract_now.py`
