# Session Log: 2025-11-21

## Summary
- **Focus:** Resolving Spring context startup failure and Login 405 Error.
- **Outcome:** Success. Application is running and login flow works.

## Key Activities

### 1. Docker Setup
- Configured `docker-compose.yml` with MariaDB 10.6, Eclipse Temurin JDK 8, Tomcat 8.5.
- Added dummy broker services using `socat`.

### 2. Application Configuration
- Created Java-based Spring configuration (`RootConfiguration`, `WebConfiguration`).
- Added `WebAppInitializer` for `DispatcherServlet` registration.
- Configured component scanning.

### 3. JNI Integration
- Initially disabled for debugging.
- Re-enabled and linked successfully.

### 4. Login Fix (The "405 Method Not Allowed" Issue)
- **Problem:** POST requests to `/login` were failing with 405.
- **Root Cause:**
    1. Missing `springSecurityFilterChain` in `web.xml`.
    2. `LoginFailureHandler` was using `forward()` (preserving POST) to a GET-only controller.
- **Solution:**
    1. Added filter to `web.xml`.
    2. Changed handler to use `response.sendRedirect()` (Post-Redirect-Get).
    3. Updated `LoginController` to accept params.

## Artifacts
- `curl` tests confirmed 302 Redirect behavior.
